// For ejemplo

cars = ["audi", "ferrari", "porsche"]
for (let i = 0; i<= cars.length; i++){ console.log("Carros almacenado en la posicion "+i+" es :"+cars[i])}

//For in Ejemplo
const object = { a: 1, b: 2, c: 3 };

for (const property in object) {
  console.log(`${property}: ${object[property]}`);
}

